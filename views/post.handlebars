<div>
    <div class="post-header">
        <h1>{{post.title}}</h1>
        <p>{{post.user.username}}</p>
        <p>{{post.updatedAt}}</p>
    </div>   
    <article>
        {{post.content}}
    </article>
</div>